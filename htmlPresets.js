let option_name;
let option_id;

let buttonPreset =
`<div class = "option">
    <label for = "${option_name}" id = "${option_name}Label"></label>
    <input type = "checkbox" id = "${option_name}Checkbox" name = "${option_name}" option_id = "${option_id}"></input>
</div>`